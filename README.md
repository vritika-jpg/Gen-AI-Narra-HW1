# Gen-AI-Narra-HW1

This repo contains the first homework assignment for my Generative AI class. I used a coding assistant (Codex) for this assignment. 

---
### Steps
1.  Initialized the repository locally with `git init`
2. Connected it to GitHub with `git remote add origin`
3. Staged files with `git add .`
4. Committed changes with `git commit -m "message"`
5. Pushed to GitHub with `git push -u origin main`

---
### Video Link
[Unlisted Video Link Here](https://www.youtube.com/watch?v=LJQHvcsmo_s)

